Install Pyspark- https://www.youtube.com/watch?v=QYTPpqPYaw0


command to start pyspark in vs code.
py -3.9 -m venv .test_env
.test_env\scripts\activate
pip install pyspark
pyspark
.test_env\scripts\deactivate

Intro to pyspark- https://medium.com/the-researchers-guide/introduction-to-pyspark-a61f7217398e


Spark in shell with scala
spark-shell
val df=spark.range(0,10)
df.show()


kafka-mysql 
>>> from kafka import KafkaProducer
>>> import mysql.connector
>>> from json import dumps
>>> connection=mysql.connector.connect(host="localhost",database="office",user="root",password="mysqlpassword")
>>> cursor.execute("select * from employee")



KafkaProducer(bootstrap_servers=['localhost:9092'],
              api_version=(0,11,5),
              value_serializer=lambda x: dumps(x).encode('utf-8'))


